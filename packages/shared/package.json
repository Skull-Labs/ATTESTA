{
  "name": "@attesta/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared types and constants for Attesta",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
