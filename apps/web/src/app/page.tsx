export default function Home() {
  return (
    <main style={{ padding: "2rem", fontFamily: "system-ui, sans-serif" }}>
      <h1>Attesta</h1>
      <p>
        Frontend template — replace this page with the product UI (landing,
        roles, mint, verify).
      </p>
    </main>
  );
}
